<?php

declare(strict_types=1);

/**
 * Rendered by \Netresearch\WebConsole\WebConsole::renderPage() once a
 * credential is configured. Loads the terminal shell. Locals:
 *
 * @var string $head shared <head> fragment (favicon, viewport, title)
 * @var string $css  pre-bundled CSS (normalize + terminal + project styles)
 * @var string $js   pre-bundled JS  (jquery + mousewheel + terminal + app)
 */
?><!DOCTYPE html>
<html class="no-js">
    <head>
        <?php echo $head; ?>
        <style type="text/css"><?php echo $css; ?></style>
        <script type="text/javascript"><?php echo $js; ?></script>
    </head>
    <body></body>
</html>
